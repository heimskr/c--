n: i32 = 38 + 4;
empty: bool;

fn triple_and_add(in: i32, addend: i32): i32 {
	return in << 1 + in + addend;
}

fn main(): void {
	// Functions whose names are "$" followed by a type are built-in print functions.
	$i32(n);
	$s(" * 3 + 10 = ");
	$i32(triple_and_add(n, 10));
	$c('\n');
	return 0;
}
